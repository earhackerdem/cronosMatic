# CronosMatic – Backend

FastAPI backend for CronosMatic.

## Quick start

```bash
# Install dependencies
uv sync

# Run dev server
uv run uvicorn app.main:app --reload

# Run tests
uv run pytest

# Lint
uv run ruff check .
```
